# No environment variables are required for this project by default.
#
# If you later add any Vite environment variables, they must be prefixed with `VITE_`
# to be exposed to the frontend build (do not put secrets in `VITE_*` variables).
